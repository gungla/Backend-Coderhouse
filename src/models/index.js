const { User } = require('./User');
const { Product } = require('./Product');
const { Cart } = require('./Cart');
const { Order } = require('./Order');
const { Message } = require('./Message');

module.exports = {
  User, Product, Cart, Order, Message,
};
